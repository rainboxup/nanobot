# Enterprise Packaging Bootstrap

This workspace was initialized with the `enterprise` packaging profile.

Suggested next steps:

1. Review `docs/howto/enterprise-bundle-bootstrap.md`
2. Confirm readiness in `/api/ready` (`packaging_ready=true`)
3. Validate operator surfaces for users, security, and ops before rollout
